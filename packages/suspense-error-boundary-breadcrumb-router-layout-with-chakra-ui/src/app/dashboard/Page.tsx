import * as f from '~/common/lib/f.css'
import styled from '@emotion/styled'

export default function DashboardPage() {
  return <Block>DashboardPage</Block>
}

const Block = styled.div`
  ${f.fullWidthHeight}
  padding: 1rem;
  border: 1px solid wheat
`
